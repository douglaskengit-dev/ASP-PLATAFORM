/**
 * OBSOLETO — não usar.
 *
 * Este arquivo continha o TR padrão de 11 itens usado na primeira versão da
 * análise de TR (comparação item-a-item com "aceitar/recusar sugestão").
 * Essa lógica foi substituída pela especificação funcional v1.1
 * (docs/ESPECIFICAÇÃO FUNCIONAL DO SISTEMA - v1.1.docx), que usa 8 itens de
 * análise próprios, implementados em lib/tr/*.
 *
 * Mantido apenas como stub porque o ambiente de edição não permite excluir
 * arquivos do projeto do usuário; sem nenhuma referência ativa no código.
 */
export {};
